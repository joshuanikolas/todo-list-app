<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Todo App Show Page</title>
  </head>
  <body>
    <%- include('../partials/_navbar.ejs') %>
    <a href="/users/:userId/todos/">Back to My Tasks</a>
    <h1><%= todo.name %></h1>
    <p>Date: <%= todo.date %></p>
    <p>Notes: <%= todo.notes %></p>
    <% if (todo.isFinished) { %>
        <p>This task is completed!</p>
      <% } else { %>
        <p>This task is not completed!</p>
      <% } %>

    <form action="/users/<%= user._id %>/todos/<%= todo._id %>?_method=DELETE"
      method="POST">
        <button type="submit">Delete This Task</button>
    </form>
    <a href="/users/<%= user._id %>/todos/<%= todo._id %>/edit">Edit this application</a>
    <a href="/users/:userId/todos">Back to Tasks</a>
    </form>
   
  </body>
</html>