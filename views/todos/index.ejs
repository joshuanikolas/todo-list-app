<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <title>Todo App</title>
  </head>
  <body>
    <%- include('../partials/_navbar.ejs') %>
    
        <div class="task-headers-intro">
    <h1>Welcome to Todo List App!</h1>
        
    <p>An app for organization and having all your tasks in one app.</p>
        </div>
        <div class="h2">
      <h2>Your Current Tasks</h2>
        </div>
              <div class="router-buttons">
            <a class="add-task" href="/users/<%= currentUser._id %>/todos/new">Add New task</a>
                </div>
                <div class="tasks-container">
            <% todos.forEach(todo =>{ %>
              <li>
                <a href="/users/<%= currentUser._id %>/todos/<%=todo._id %>">
                <%= todo.name %> </a>
                  <ul>Date: <%= todo.date %></ul>
                  <ul>Notes: <%= todo.notes %></ul>
                  <ul>Completed? <%= todo.isFinished %></ul>
              </li>
            <% }) %> 
                </div>
    </body>
  </html>

  </body>
</html>
